import { CalificacionPipePipe } from './calificacion-pipe-pipe';

describe('CalificacionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CalificacionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
